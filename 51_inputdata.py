name=input("Enter your name")
print(name.strip())#the strip method is used to remove extra spaces
number=input("Enter the number for multiplication")
print(int(number)*2)#by default the input data is string so to use them we need to type cast it